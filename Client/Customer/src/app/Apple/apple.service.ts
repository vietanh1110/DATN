import { Injectable } from '@angular/core';
import { Observable, of } from 'rxjs';

@Injectable({
  providedIn: 'root'
})
export class AppleService {

  

  // getAppleProduct(id: any): Observable<any[]> {
  //   const apple = product.filter(x => x.brandId === 'apple' && x.categoryId === id);
  //   return of(apple)
  // }

}
