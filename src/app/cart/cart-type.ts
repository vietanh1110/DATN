export interface cartItem {
    productId: string,
    productName: string,
    priceSell: number,
    price: number,
    imagePath: string,
    totalQuantity: number,
    quantity: number,
    isSelected: boolean
}